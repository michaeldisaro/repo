{
    "configurations": [
        {
            "name": "Run",
            "command": "cargo build && cargo run",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}